# Differential Expression Module — Server

Differential Expression Module — Server

## Usage

``` r
mod_de_server(id, seurat_obj, allow_conserved = FALSE)
```

## Arguments

- id:

  Module namespace ID.

- seurat_obj:

  Reactive clustered Seurat object.

- allow_conserved:

  Logical. Whether to expose conserved-marker analysis.
