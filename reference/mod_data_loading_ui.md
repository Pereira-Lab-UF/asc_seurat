# Data Loading Module — UI

Data Loading Module — UI

## Usage

``` r
mod_data_loading_ui(id, demo_mode = FALSE)
```

## Arguments

- id:

  Module namespace ID.

- demo_mode:

  Logical. When TRUE, hides input controls (used by Demo tab).
