# Action Button (Styled)

A styled action button consistent with the v3 theme.

## Usage

``` r
action_btn(id, label, icon = NULL, class = "btn-primary")
```

## Arguments

- id:

  Input ID.

- label:

  Button label.

- icon:

  An optional icon.

- class:

  CSS class. Defaults to "btn-primary".
