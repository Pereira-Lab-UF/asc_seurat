# Clustering Module - Server

Clustering Module - Server

## Usage

``` r
mod_clustering_server(id, norm_result)
```

## Arguments

- id:

  Module namespace ID.

- norm_result:

  Reactive Seurat object or a list of reactives returned by the
  normalization module.

## Value

Reactive clustered Seurat object.
