# Data Loading Module — Server

Data Loading Module — Server

## Usage

``` r
mod_data_loading_server(id, trigger_demo = NULL)
```

## Arguments

- id:

  Module namespace ID.

- trigger_demo:

  Optional reactive value. When incremented above 0, demo data loads
  automatically (used by the Demo tab).

## Value

Reactive Seurat object.
