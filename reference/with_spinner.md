# Custom Spinner Wrapper

Wraps a UI element with a loading spinner.

## Usage

``` r
with_spinner(ui_element, hide_ui = FALSE)
```

## Arguments

- ui_element:

  A Shiny UI element.

- hide_ui:

  Logical. Whether to hide the UI while loading.

## Value

The UI element wrapped with a spinner.
